public List<Integer> evenNumbers(Integer n) {
    //code here
    Integer i;
    list<Integer> varNumList = new list<Integer>();
    for(i=1 ; i<=n; i++){
        if( math.mod(i,2) == 0){
            varNumList.add(i);
        }
   }
return varNumList;
    
}